<%- |
  String $version,
  String $flavor,
  | -%>

[Unit]
Description=Node Exporter

[Service]
User=node_exporter
EnvironmentFile=/etc/default/node_exporter
ExecStart=/opt/node_exporter-<%= $version %>.<%= $flavor %>/node_exporter $OPTIONS

[Install]
WantedBy=multi-user.target
